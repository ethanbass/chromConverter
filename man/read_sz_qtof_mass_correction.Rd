% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_shimadzu_qtof.R
\name{read_sz_qtof_mass_correction}
\alias{read_sz_qtof_mass_correction}
\title{Read the cached mass correction from a 'Shimadzu' QTOF file}
\usage{
read_sz_qtof_mass_correction(path, polarity = read_qtof_polarity(path))
}
\arguments{
\item{path}{Path to 'Shimadzu' .lcd file.}

\item{polarity}{Ion polarity, either \code{positive} or \code{negative}, selecting
which of the two blocks to read. Defaults to the polarity recorded in the
file.}
}
\value{
A list of \code{scale}, \code{offset} and \code{shift}, or \code{NULL} if the stream is
missing or holds no usable result.
}
\description{
\verb{Mass Data Load Format/Mass Correction Cache} holds the mass correction
'LabSolutions' applied to the run: the reference ions it found and the
coefficients it fitted to them. Like the other QTOF parameter streams it is
a protocol buffer with a block per polarity (field \code{10} positive, \code{20}
negative), the reference compounds under \code{10} and the result under \code{30}:
}
\details{
\itemize{
\item \verb{30.<pol>.10.10} --- offset of the correction, in flight-time units
\item \verb{30.<pol>.10.20} --- scale factor of the correction, near 1
\item \verb{30.<pol>.20.10.10}, \code{.20} --- id of a reference compound and the m/z at
which it was measured in this run, scaled by 1e9
}

The correction is applied to the flight time, as \verb{t' = scale * t + offset},
which is the same as shifting the calibration in the square root of the
mass:
\deqn{\sqrt{mz'} = scale \sqrt{mz} + shift}
where \code{shift} is the mean of \code{sqrt(theoretical) - scale * sqrt(measured)}
over the reference ions. Reading the vendor's own correction out reproduces
its mass axis exactly, where refitting on the reference ions found in the
data leaves a few tenths of a ppm.
}
\keyword{internal}
