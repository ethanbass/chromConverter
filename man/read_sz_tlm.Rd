% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_shimadzu_tlm.R
\name{read_sz_tlm}
\alias{read_sz_tlm}
\title{Read 'Shimadzu' TLM stream}
\usage{
read_sz_tlm(
  path,
  format_out = c("matrix", "data.frame", "data.table"),
  data_format = "long",
  levels = c("MS1", "MS2"),
  sparse = TRUE,
  read_metadata = TRUE,
  metadata_format = "shimadzu_lcd"
)
}
\arguments{
\item{path}{Path to \code{.lcd} file.}

\item{format_out}{Class of output. Either \code{data.table} or
\code{data.frame}. Spectra are long and mixed-type, so \code{matrix}
resolves to \code{data.table}.}

\item{data_format}{Ignored: spectra have no wide representation and are
always returned in \code{long} format.}

\item{levels}{Which MS levels to return, spelled \code{MS1} and \code{MS2}. Both are
decoded either way, because the level of a scan is recorded inside its own
compressed record.}

\item{sparse}{Logical. Whether to return spectra in sparse format
(excluding zeros), as \code{\link[=call_rainbow]{call_rainbow()}} does. Defaults to \code{TRUE}, since a
profile scan is mostly empty and keeping its zeros costs roughly an order
of magnitude more memory.}

\item{read_metadata}{Logical. Whether to attach metadata.}

\item{metadata_format}{Format to output metadata.}
}
\value{
A named list holding whichever of the requested \code{levels} the file
has, each a long table with columns \code{scan}, \code{rt} (minutes), \code{mz} and
\code{intensity}, and a \code{precursor_mz} column before \code{mz} where the scans carry
one. A per-scan summary
(retention time, event, MS level, polarity, precursor m/z, point count) is
attached to each as a \code{scan_info} attribute.
}
\description{
Read the \verb{TLM Raw Data} mass spectrometry stream from 'Shimadzu
LabSolutions' \code{.lcd} files.
}
\details{
Spectra live in the \verb{MS Raw Data} stream and are located through
\verb{Spectrum Index}, which holds one 24-byte entry per spectrum:\tabular{lll}{
   \strong{Offset} \tab \strong{Type} \tab \strong{Field} \cr
   0–3 \tab \code{uint32} \tab Record size in bytes \cr
   4–7 \tab \code{uint32} \tab Event number \cr
   8–11 \tab \code{uint32} \tab Offset into \verb{MS Raw Data} \cr
   12–15 \tab \code{uint32} \tab Padding \cr
   16–19 \tab \code{uint32} \tab Cycle number \cr
   20–23 \tab \code{uint32} \tab Scan number \cr
}


Each record begins with a 12-byte wrapper (\code{0xFFFFFFFF}, uncompressed size,
compressed size) followed by a zlib stream. The decompressed block opens
with a 44-byte header shared by every scan type:\tabular{lll}{
   \strong{Offset} \tab \strong{Type} \tab \strong{Field} \cr
   0–3 \tab \code{uint32} \tab Retention time (milliseconds) \cr
   4–7 \tab \code{uint32} \tab Retention time of the first scan in the cycle \cr
   8–11 \tab \code{uint32} \tab Event number \cr
   12–15 \tab \code{uint32} \tab Scan counter within the event \cr
   16–19 \tab \code{uint32} \tab Scan index (0-based) \cr
   20–21 \tab \code{uint16} \tab Scan type: 10 = MS1 profile, 11 = SIM, 14 = MS2 profile, 15 = MRM \cr
   22–23 \tab \code{uint16} \tab MS level + 1 (0 in a truncated final scan) \cr
   24–27 \tab \code{uint32} \tab Constant (\code{0x00010000}) \cr
   28–31 \tab \code{uint32} \tab Last precursor m/z x 100 (stale outside product-ion scans) \cr
   32–35 \tab \code{uint32} \tab Instrument state bit field; bit 0 follows polarity \cr
   36–39 \tab \code{uint32} \tab Polarity (0 = positive, 1 = negative) \cr
   40–43 \tab \code{uint32} \tab Number of data points (profile) or transitions (MRM, SIM) \cr
}


\strong{Profile scans} (type 10 and 14) follow the header with two m/z pairs
stored as m/z x 100 — the isolation window (equal low and high values for
MS2; the scan range for MS1), then the scan range — and then \code{n}
unsigned 32-bit intensities. Bit 31 of an intensity marks detector
saturation and is not part of the value. Intensities are already in the
units LabSolutions reports, so no scaling is applied.

Points lie on an evenly spaced m/z grid running from the low end of the scan
range, with a bin width of \code{(high - low) / (100 * n)} (0.1 in the files
seen so far). The grid overhangs the acquired range by 10 bins at the bottom
and 9 at the top; those are dropped, which is what makes the summed
intensity match the \verb{TIC Data} stream exactly.

\strong{MRM and SIM scans} (types 15 and 11) instead follow the header with \code{n}
12-byte transitions: Q1 m/z x 100, Q3 m/z x 100, and intensity.
}
\author{
Ethan Bass
}
\keyword{internal}
