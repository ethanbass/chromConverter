% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chrom_list-methods.R
\name{print.chrom_list}
\alias{print.chrom_list}
\title{Print a chrom_list object}
\usage{
\method{print}{chrom_list}(x, n = 10, cols = chrom_summary_cols(), ...)
}
\arguments{
\item{x}{A \code{chrom_list} object.}

\item{n}{Integer. Maximum number of chromatograms to show in the table.
Defaults to \code{10}.}

\item{cols}{Character vector of attribute names to report. Defaults to:
\code{sample_name}, \code{run_datetime}, \code{method}, \code{detector}, \code{wavelength}, \code{detector_range}, \code{scan_type}, \code{polarity}, \code{precursor_mz}, \code{product_mz}, \code{mz_range}.}

\item{...}{Additional arguments (currently ignored).}
}
\value{
Invisibly returns \code{x}.
}
\description{
Prints a summary of a \code{chrom_list} without displaying the underlying
chromatographic data. Attributes that are constant across all chromatograms
are collapsed into a single header line, while varying attributes are shown
as a table truncated to the first \code{n} rows. When a sample holds more than
one chromatogram, its traces are printed as a block headed by the sample,
and any attribute they all share is shown in that block's header rather
than repeated down it.
}
\seealso{
\link{extract_metadata}
}
