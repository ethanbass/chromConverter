% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_shimadzu_qtof.R
\name{read_sz_qtof}
\alias{read_sz_qtof}
\title{Read 'Shimadzu' QTOF stream}
\usage{
read_sz_qtof(
  path,
  format_out = "data.frame",
  data_format = "wide",
  read_metadata = TRUE,
  metadata_format = "shimadzu_lcd",
  scale = TRUE
)
}
\arguments{
\item{mz_hex}{Character. Hex string of the m/z block (int64 flight times).}

\item{int_hex}{Character. Hex string of the intensity block (uint16 counts).}

\item{n}{Integer. Number of peaks. If NULL, inferred from block sizes.}

\item{A}{Numeric. TOF calibration coefficient A (default fitted from calibrants).}

\item{B}{Numeric. TOF calibration coefficient B (default fitted from calibrants).}

\item{int_scale}{Numeric. Intensity scale factor (default 3.761).}
}
\value{
A data.table with columns \code{mz} and \code{intensity}.
}
\description{
Read QTOF stream from 'Shimadzu LabSolutions' \code{.lcd} files.
}
\details{
Data for each scan is stored in three contiguous blocks: a 64-byte header,
a block of flight times, followed by a block of intensities.

\strong{Scan Header} (64 bytes, little-endian):\tabular{lll}{
   \strong{Offset} \tab \strong{Type} \tab \strong{Field} \cr
   0–3 \tab \code{uint32} \tab MS level (1 = MS1, 2 = MS2) \cr
   4–7 \tab \code{uint32} \tab Retention time (milliseconds) \cr
   8–15 \tab \code{int64} \tab Base peak flight time (TOF ticks; 0 if empty scan) \cr
   16–19 \tab \code{uint32} \tab Base peak raw intensity (0 if empty scan) \cr
   20–23 \tab \code{uint32} \tab Scan index (0-based; increments for every scan including empty ones) \cr
   24–27 \tab \code{uint32} \tab Data block size in bytes (\code{n_peaks * 10}; 0 if empty scan) \cr
   28–31 \tab \code{uint32} \tab Flags: \code{0x00010001} = scan with data; \verb{0x000200xx} = empty scan \cr
   32–35 \tab \code{uint32} \tab Padding \cr
   36–39 \tab \code{uint32} \tab Spectrum count (2 = has data, 1 = empty) \cr
   40–43 \tab \code{uint32} \tab Instrument constant (scan window / detector setting) \cr
   44–47 \tab \code{uint32} \tab Instrument constant \cr
   48–51 \tab \code{uint32} \tab Instrument constant \cr
   52–55 \tab \code{uint32} \tab Instrument constant \cr
   56–63 \tab — \tab Padding \cr
}


\strong{m/z block} (n x 8 bytes)
Each peak's m/z is stored as a signed 64-bit little-endian integer
representing a TOF flight time in instrument ticks. Conversion uses a
two-parameter calibration derived from the TOF \code{Calibration Table}
stream:
\deqn{mz = ((t-B)/A)^2}
where \code{A} and \code{B} are derived from the calibrant flight times.

\strong{Intensity block} (n x m bytes)
Each peak's intensity is stored as an unsigned 16-bit little-endian integer
(raw detector counts). To convert to the intensity values reported by
LabSolutions, values must be divide by a fixed hardware gain factor of approximately 3.761.
}
