% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/attach_metadata.R
\name{extract_metadata}
\alias{extract_metadata}
\title{Extract metadata}
\usage{
extract_metadata(
  chrom_list,
  what = chrom_metadata_fields(),
  detector = NULL,
  format_out = c("data.frame", "data.table", "tibble"),
  collapse = FALSE,
  expand = FALSE
)
}
\arguments{
\item{chrom_list}{A list of chromatograms with attached metadata (as returned
by \code{read_chroms} with \code{read_metadata = TRUE}), or a single chromatogram.
Nested lists are flattened, one row per chromatogram.}

\item{what}{A character vector specifying the metadata elements to
extract. Defaults to every field chromConverter attaches; no format
records all of them, so the elements a format does not provide are
absent from the result. Superseded names (e.g. \code{injection_volume},
\code{software_name}, \code{time_start}) are accepted and mapped to the names
that replaced them. An element of a nested field (see \code{expand}) may be
named too, either by the column it is reported under (\code{SampleLabel}) or in
full (\code{acaml_metadata.SampleLabel}), to report it without the rest of its
field. A field requested by name that no chromatogram carries produces a
warning.}

\item{detector}{A character vector of detectors to include (e.g. \code{"UV"} or
\code{c("UV", "MS")}), matched case-insensitively against each chromatogram's
\code{detector} attribute. Defaults to \code{NULL}, in which case all chromatograms
are included. Useful for lists containing more than one detector per
sample. It is an error if no chromatogram matches.}

\item{format_out}{Format of object. Either \code{data.frame}, \code{data.table} or
\code{tibble}.}

\item{collapse}{Logical. Whether to collapse a field holding more than one
value (\code{time_range}, or the \code{product_mz} of an MRM event monitoring several
transitions) into a single comma-separated string. Defaults to \code{FALSE}, in
which case such a field is spread across numbered columns
(\code{time_range1}, \code{time_range2}).}

\item{expand}{Whether to include the nested metadata fields, whose value is
itself a list or table rather than a single value per chromatogram: the
\code{ms_params} instrument settings, the \code{acaml_metadata} injection record that
\code{read_agilent_rslt} reads from the \code{.acaml} file, or the whole vendor list
that \code{metadata_format = "raw"} passes through. Either \code{TRUE}, to include
every nested field the chromatograms carry, a character vector naming the
ones to include, or \code{FALSE} (the default) to include none. Each element
becomes a column of its own, named for itself (\code{SampleName}) unless that
name is already taken, in which case it carries the field it came from
(\code{ms_params.polarity}, since \code{polarity} is a metadata field in its own
right).}
}
\value{
A \code{data.frame}, \code{tibble}, or \code{data.table} (according to the value of
\code{format_out}), with one row per chromatogram and the specified metadata
elements as columns, or \code{NA} if none of the specified elements could be
found. For a list, the first column, \code{name}, identifies each chromatogram
by its path through the list (e.g. \code{blue.UV}).
}
\description{
Extract metadata as a \code{data.frame}, \code{data.table} or \code{tibble} from a list of
chromatograms.
}
\examples{
path <- system.file("extdata/ladder.txt", package = "chromConverter")
chroms <- read_chroms(path, format_in = "shimadzu_ascii",
                      find_files = FALSE, progress_bar = FALSE)
extract_metadata(chroms, what = c("sample_name", "instrument", "run_datetime"))
}
