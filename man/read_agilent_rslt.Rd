% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_agilent_rslt.R
\name{read_agilent_rslt}
\alias{read_agilent_rslt}
\title{Read files from 'Agilent' .rslt directories}
\usage{
read_agilent_rslt(
  path,
  what = c("chroms", "dad"),
  path_out = NULL,
  format_out = c("matrix", "data.frame", "data.table"),
  data_format = c("wide", "long"),
  read_metadata = TRUE,
  metadata_format = c("chromconverter", "raw"),
  collapse = TRUE,
  sample_names = c("basename", "sample_name"),
  progress_bar = FALSE,
  cl = 1
)
}
\arguments{
\item{path}{Path to 'Agilent' \code{.rslt} directory.}

\item{what}{Whether to extract chromatograms (\code{chroms}), DAD data
(\code{dad}) and/or auxiliary instrumental data (\code{instrument}), such as
temperature, pressure or solvent composition. Accepts multiple arguments,
and defaults to \code{chroms} and \code{dad}. When more than one is requested, any the archive does not contain
are left out; a single one that is missing is an error.}

\item{path_out}{A directory to export unzipped files. If a path is not
specified, a temporary directory is used. The files are extracted into a
folder named for \code{path}, overwriting any files of the same name already
there.}

\item{format_out}{Class of output. Either \code{matrix}, \code{data.frame}, or
\code{data.table}.}

\item{data_format}{Whether to return data in \code{wide} (default) or \code{long} format.}

\item{read_metadata}{Logical. Whether to attach metadata. Defaults to \code{TRUE}.}

\item{metadata_format}{Format to output metadata. Either \code{chromconverter}
(standardized field names) or \code{raw} (vendor field names, unmapped).}

\item{collapse}{Logical. Whether to collapse lists that only contain a single
element. Defaults to \code{TRUE}.}

\item{sample_names}{How to name the chromatograms that are returned. Either
\code{basename} (default), to use the name of the source \code{.dx} file, or
\code{sample_name}, to use the sample name field from the metadata.}

\item{progress_bar}{Logical. Whether to show a progress bar while reading
the sequence's \code{.dx} files. Defaults to \code{FALSE}, since \link{read_chroms} already
reports progress over the directories it found and a second bar inside each
one would be redrawn per directory. Set to \code{TRUE} when calling this function
directly on a sequence with many injections.}

\item{cl}{Argument to \link[pbapply:pbapply]{pbapply} specifying the number
of clusters to use or a cluster object created by
\link[parallel:makeCluster]{makeCluster}. Defaults to \code{1}.}
}
\value{
A list of chromatograms (one \code{read_agilent_dx}-style result per
injection in the sequence), in the format specified by \code{data_format} and
\code{format_out}. If \code{read_metadata} is \code{TRUE}, injection-level metadata parsed
from the \code{.acaml} file is attached to each chromatogram as attributes.
}
\description{
Reads a sequence of injections from an 'Agilent' \code{.rslt} directory.
Combines raw chromatogram data from \code{.dx} files with injection-level
metadata parsed from the accompanying \code{.acaml} file.
}
\details{
Currently this function only reads \code{.dx} chromatogram files. Peak tables
stored in \code{.rx} files are not yet supported.
}
\examples{
\dontrun{
read_agilent_rslt("path/to/sequence.rslt")
}
}
\seealso{
Other 'Agilent' parsers:
\code{\link[=read_agilent_d]{read_agilent_d()}},
\code{\link[=read_agilent_dx]{read_agilent_dx()}},
\code{\link[=read_chemstation_ch]{read_chemstation_ch()}},
\code{\link[=read_chemstation_csv]{read_chemstation_csv()}},
\code{\link[=read_chemstation_ms]{read_chemstation_ms()}},
\code{\link[=read_chemstation_reports]{read_chemstation_reports()}},
\code{\link[=read_chemstation_uv]{read_chemstation_uv()}}
}
\author{
Ethan Bass
}
\concept{'Agilent' parsers}
